# UMP Packet Inspector

Command-line utility for inspecting UMP packets.

## Features

- Inspect packet metadata
- Decode packet headers
- Display fragmentation information
- Display raw payload information
- Read JSON packet representations
- Output human-readable or JSON results

## Usage

```text
python inspect.py packet.json
python inspect.py packet.json --json
python inspect.py packet.json --header
