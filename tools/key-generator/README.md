# UMP Key Generator

Command-line utility for generating UMP development key material.

## Features

- Generate random symmetric keys
- Generate development identity key material
- Export keys as JSON
- Export keys as hexadecimal strings

## Usage

```text
python generate.py key
python generate.py key --size 32
python generate.py identity
python generate.py identity --format json
